// import vue from 'vue/dist/vue.esm.browser'
export default ({
  Vue, // The Vue constructor used by VuePress
  options, // Additional options attached to the root instance
  router, // The router instance for the current app
  siteData // Site metadata
}) => {
  // window.Vue = vue // Expose the Vue constructor globally (enable Vue demos on pages)
}
